% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinal_reg-clm.R
\name{clm_wrapper}
\alias{clm_wrapper}
\title{Fit wrapper for \code{ordinal::clm()}}
\usage{
clm_wrapper(
  formula,
  data,
  weights = NULL,
  parallel_reg = NULL,
  link = NULL,
  threshold = NULL,
  ...,
  call = rlang::caller_env()
)
}
\arguments{
\item{formula}{The formula to pass.}

\item{data}{The data frame to pass.}

\item{weights}{An optional vector of case weights to pass.}

\item{parallel_reg}{Logical; whether predictor effects are shared across
thresholds. When \code{FALSE}, every predictor is moved to a \code{nominal} formula.}

\item{...}{Additional arguments to pass.}

\item{call}{The execution environment of a currently running function.}
}
\value{
An object of class \code{clm} as returned by \code{\link[ordinal:clm]{ordinal::clm()}}.
}
\description{
Fit \code{clm()}, translating \code{parallel_reg} into the \code{nominal} formula. This is
done here rather than in \code{\link[parsnip:translate]{parsnip::translate()}} because the formula can only
be split once \code{data} is known, and because the standard translation
(\code{make_form_call()}) overwrites any location formula that \code{translate()} sets.
}
\examples{
\dontshow{if (rlang::is_installed("MASS") && rlang::is_installed("ordinal")) withAutoprint(\{ # examplesIf}
house_data <-
  MASS::housing[rep(seq(nrow(MASS::housing)), MASS::housing$Freq), -5]
# arguments are translated
( fit_orig <- ordinal::clm(
  Sat ~ Type + Cont, data = house_data,
  link = "logit", threshold = "symmetric2"
) )
( fit_wrap <- clm_wrapper(
  Sat ~ Type + Cont, data = house_data,
  link = "logistic", threshold = "symmetric_zero"
) )
# relax the parallel regression assumption
( fit_orig <- ordinal::clm(
  Sat ~ 1, data = house_data,
  nominal = ~ Infl + Cont
) )
( fit_wrap <- clm_wrapper(
  Sat ~ Infl + Cont, data = house_data,
  parallel_reg = FALSE
) )
\dontshow{\}) # examplesIf}
}
\keyword{internal}
