% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param-engine-orf.R
\name{orf_parameters}
\alias{orf_parameters}
\alias{sample_fraction}
\alias{honesty}
\alias{honesty_fraction}
\title{Dials for \code{orf} engine parameters}
\usage{
sample_fraction(range = c(0.1, 1), trans = NULL)

honesty(values = c(TRUE, FALSE))

honesty_fraction(range = c(0.1, 0.9), trans = NULL)
}
\arguments{
\item{range}{A two-element vector holding the \emph{defaults} for the smallest and
largest possible values, respectively. If a transformation is specified,
these values should be in the \emph{transformed units}.}

\item{trans}{A \code{trans} object from the \code{scales} package, such as
\code{scales::transform_log10()} or \code{scales::transform_reciprocal()}. If not
provided, the default is used which matches the units used in \code{range}. If
no transformation, \code{NULL}.}

\item{values}{A logical vector of possible values. See \code{values_ord_metric}.}
}
\value{
An object of S3 parent class \code{param} and primary class \code{qual_param}
or \code{quant_param}; see \code{\link[dials:new_qual_param]{dials::new_qual_param()}} and
\code{\link[dials:new_quant_param]{dials::new_quant_param()}}.
}
\description{
These parameters are auxiliary to random forest models that use the
"orf" engine. They correspond to tuning parameters that would be
specified using \code{set_engine("orf", ...)}.
}
\details{
These functions generate parameters for \code{\link[parsnip:rand_forest]{parsnip::rand_forest()}}
models using the \code{"orf"} engine. See \code{?orf::orf()} for more details on
the original parameters. These parameters are engine-specific, not general
to random forest models, so are provided here rather than in \code{dials}.
\itemize{
\item \code{sample_fraction()}: The fraction of observations to be subsampled;
tunes \code{sample.fraction}.
\item \code{honesty()}: Whether to use honest splitting (sample splitting);
tunes \code{honesty}.
\item \code{honesty_fraction()}: The fraction of observations reserved for the
honest (estimation) sample; tunes \code{honesty.fraction}.
}
}
\examples{
sample_fraction()
honesty()
honesty_fraction()

}
\seealso{
\code{\link[dials:trees]{dials::trees()}}
}
