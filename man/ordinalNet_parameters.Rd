% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param-engine-ordinalNet.R
\name{ordinalNet_parameters}
\alias{ordinalNet_parameters}
\alias{parallel_penalty_factor}
\title{Dials for \code{ordinalNet} engine parameters}
\usage{
parallel_penalty_factor(
  range = c(-Inf, Inf),
  trans = scales::transform_log10()
)
}
\arguments{
\item{range}{A two-element vector holding the \emph{defaults} for the smallest and
largest possible values, respectively. If a transformation is specified,
these values should be in the \emph{transformed units}.}

\item{trans}{A \code{trans} object from the \code{scales} package, such as
\code{scales::transform_log10()} or \code{scales::transform_reciprocal()}. If not
provided, the default is used which matches the units used in \code{range}. If
no transformation, \code{NULL}.}
}
\value{
An object of S3 parent class \code{param} and primary class
\code{quant_param}; see \code{\link[dials:new_quant_param]{dials::new_quant_param()}}.
}
\description{
These parameters are auxiliary to ordinal regression models that use the
"ordinalNet" engine. They correspond to tuning parameters that would be
specified using \code{set_engine("ordinalNet", ...)}.
}
\details{
These functions generate parameters for \code{\link[parsnip:ordinal_reg]{parsnip::ordinal_reg()}}
models using the \code{"ordinalNet"} engine. See \code{?ordinalNet::ordinalNet()} for
more details on the original parameters. These parameters are
engine-specific, not general to ordinal regression models, so are provided
here rather than in \code{dials}.
\itemize{
\item \code{parallel_penalty_factor()}: A nonnegative numeric factor that scales the
penalty on all parallel terms; tunes \code{parallelPenaltyFactor}. Only used
when \code{parallelTerms = TRUE}.
}
}
\examples{
parallel_penalty_factor()

}
\seealso{
\code{\link[dials:penalty]{dials::penalty()}}
}
