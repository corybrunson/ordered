% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinal_reg-VGAM.R
\docType{data}
\name{VGAM_vglm_wrapper}
\alias{VGAM_vglm_wrapper}
\alias{VGAM_vgam_wrapper}
\alias{values_ordinal_link_VGAM}
\title{Wrappers for \code{VGAM}}
\format{
An object of class \code{character} of length 10.
}
\usage{
VGAM_vglm_wrapper(
  formula,
  data,
  family = "cumulative_link",
  link = "logistic",
  parallel = TRUE,
  ...
)

VGAM_vgam_wrapper(
  formula,
  data,
  family = "cumulative_link",
  link = "logistic",
  parallel = TRUE,
  ...
)

values_ordinal_link_VGAM
}
\arguments{
\item{formula}{The formula to pass.}

\item{data}{The data frame to pass.}

\item{...}{Additional arguments to pass.}
}
\description{
These wrappers convert the standardized \code{odds_link} options encoded in
\code{\link[dials:ordinal_link]{dials::values_odds_link}} to \code{\link[VGAM:vglmff-class]{VGAM::vglmff}} objects passed to the
\code{family} argument of \code{\link[VGAM:vglm]{VGAM::vglm()}} and \code{\link[VGAM:vgam]{VGAM::vgam()}}.
}
\details{
Note that \code{VGAM::vglm()} and \code{VGAM::vgam()} treat the rows of \code{data}
as the units of observation: Compressed \code{data} with one row per predictor
set will be understood as having a multinomial-valued outcome; only
expanded data with one row per case will be understood as having a
single-valued ordinal outcome. (This divide cannot be bridged by passing a
column of counts to the \code{weights} argument.) These wrappers require a
single ordinal outcome column and therefore do not accept the convenient
\code{cbind(y1, y2, ...) ~ x1 + x2 + ...} encoding commonly used in \code{VGAM}.
}
\examples{
values_ordinal_link_VGAM
dials::ordinal_link(values = values_ordinal_link_VGAM)
\dontshow{if (rlang::is_installed("MASS") && rlang::is_installed("VGAM")) withAutoprint(\{ # examplesIf}
house_data <-
  MASS::housing[rep(seq(nrow(MASS::housing)), MASS::housing$Freq), -5]
# fit wrapper for linear model
( fit_orig <- VGAM::vglm(
  Sat ~ Type + Infl + Cont,
  family = VGAM::sratio(link = "probitlink", parallel = TRUE),
  data = house_data
) )
( fit_wrap <- VGAM_vglm_wrapper(
  Sat ~ Type + Infl + Cont,
  family = "stopping_ratio", link = "probit",
  data = house_data
) )
# fit wrapper for additive model
( fit_orig <- VGAM::vgam(
  Sat ~ Type + Infl + Cont,
  family = VGAM::cratio(link = "clogloglink", parallel = TRUE),
  data = house_data
) )
( fit_wrap <- VGAM_vgam_wrapper(
  Sat ~ Type + Infl + Cont,
  family = "continuation_ratio", link = "cloglog",
  data = house_data
) )
\dontshow{\}) # examplesIf}
}
\keyword{datasets}
\keyword{internal}
